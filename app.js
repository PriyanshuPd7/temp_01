// npm - global command, comes with node
// npm --version (to check the version)

// local dependency - use it only in this particular Project
// npm i <packagename>

// gloabal dependency - use it in any project 
// npm install -g <packagename>
// sudo npm install -g <packagename> (mac)

// package.jason - manifest file (stores important info about project/package)
// mannual approach (create project.jason int the root, create properties etc )
// npm init (step by step,press enter to skip)
// npm init -y (everything default)


// const _ = require('lodash')

// const item = [1,[2,[3,[4]]]]

// const newItems = _.flattenDeep(item)

// console.log(newItems)

